# Tiny TFTP Client -- A header-only TFTP client

This repo contains a single header (ttftp.h) that contains a small TFTP client written in C99.

It is portable to most systems that implement a BSD sockets API with support for UDP/IP. Though I haven't tested it on _everything_ yet (e.g. SunOS...)
